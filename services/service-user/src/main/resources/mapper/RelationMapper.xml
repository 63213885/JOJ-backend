<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.joj.user.relation.mapper.RelationMapper">

    <insert id="insertFollowing">
        INSERT INTO user_follow (id, from_user_id, to_user_id, rel_status, create_time, update_time)
        VALUES (#{id}, #{fromUserId}, #{toUserId}, #{relStatus}, NOW(3), NOW(3))
            ON DUPLICATE KEY UPDATE rel_status=VALUES(rel_status), update_time=VALUES(update_time)
    </insert>
    <update id="cancelFollowing">
        UPDATE user_follow SET rel_status=0, update_time=NOW(3)
        WHERE from_user_id=#{fromUserId} AND to_user_id=#{toUserId}
    </update>

    <select id="existsFollowing" resultType="int">
        SELECT COUNT(1) FROM user_follow WHERE from_user_id=#{fromUserId} AND to_user_id=#{toUserId} AND rel_status=1
    </select>

    <select id="listFollowing" resultType="long">
        SELECT to_user_id FROM user_follow WHERE from_user_id=#{fromUserId} AND rel_status=1
        ORDER BY create_time DESC LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="listFollowers" resultType="long">
        SELECT from_user_id FROM user_follow WHERE to_user_id=#{toUserId} AND rel_status=1
        ORDER BY create_time DESC LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="listFollowingRows" resultType="map">
        SELECT to_user_id AS toUserId, create_time AS createTime
        FROM user_follow
        WHERE from_user_id=#{fromUserId} AND rel_status=1
        ORDER BY create_time DESC LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="listFollowerRows" resultType="map">
        SELECT from_user_id AS fromUserId, create_time AS createTime
        FROM user_follow
        WHERE to_user_id=#{toUserId} AND rel_status=1
        ORDER BY create_time DESC LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countFollowingActive" resultType="int">
        SELECT COUNT(1) FROM following WHERE from_user_id=#{fromUserId} AND rel_status=1
    </select>

    <select id="countFollowerActive" resultType="int">
        SELECT COUNT(1) FROM user_follow WHERE to_user_id=#{toUserId} AND rel_status=1
    </select>

</mapper>